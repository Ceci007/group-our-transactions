<div class="d-flex justify-content-center text-light bg-primary p-2 margin-negative">
    <h4 class="font-weight-light mt-2">REGISTER</h4>
</div>
<br />
<%= render 'layouts/flash' %>
<%= user_error_msgs(@user) %>
<h2 class="text-center">Sign up</h2>
<div class="row justify-content-center">
    <div class="col-4">
        <%= form_with scope: :user, url: users_path, local: true do |f| %>
             <div class="form-row">
                 <div class="col">
                     <div class="form-group">
                           <%= f.label :username %>
                           <%= f.text_field :username, autofocus: true, autocomplete: 'user name', placeholder: "User name" , class: 'form-control' %>
                     </div>
                     <div class="form-group">
                        <label for="email"><%= f.label :email %></label>
                        <%= f.email_field :email, class: "form-control shadow rounded", id: "email", placeholder: "Enter your email address" %>
                     </div>
                     <div class="form-group">
                          <%= f.submit "Sign up", class: 'btn btn-secondary btn-block' %>
                     </div>
                 </div>
             </div>
        <% end %>
        <p>Or <%= link_to "Login", login_path, class: "link" %></p>
    </div>
</div