<h3 class="text-center">Decors for <%=@category.name %></h3>
<% if @category.decors.empty? %>
    <h4 class="text-center">No decors available for this category.</h4>
<% else %>
    <%= render partial: 'categorydecor', collection: @decors, as: :decor  %>
<% end %>